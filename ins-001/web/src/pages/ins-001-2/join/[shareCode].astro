---
/**
 * INS-001.2 Join Game Page
 *
 * Entry point for share link recipients
 */

// This route must be server-rendered since share codes are dynamic
export const prerender = false;

import InstrumentLayout from '../../../layouts/InstrumentLayout.astro';
import JoinBridgingGame from '../../../components/bridging/JoinBridgingGame';

const { shareCode } = Astro.params;
---

<InstrumentLayout id="INS-001.2" title="SEMANTIC BRIDGING" version="0.1" status="live">
  <JoinBridgingGame shareCode={shareCode || ''} client:load />
</InstrumentLayout>
