---
/**
 * INS-001.1 Join Game Page
 *
 * Entry point for share link recipients
 */

// This route must be server-rendered since tokens are dynamic
export const prerender = false;

import InstrumentLayout from '../../../../layouts/InstrumentLayout.astro';
import JoinGame from '../../../../components/JoinGame';

const { token } = Astro.params;
---

<InstrumentLayout id="INS-001.1" title="SIGNAL" version="0.1" status="live">
  <JoinGame token={token || ''} client:load />
</InstrumentLayout>
